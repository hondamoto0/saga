const styles = theme => ({
  taskboard: {},
  shape: {
    // backgroundColor:"red",
    // color:"white",
    // borderColor:"#ccccc",
    backgroundColor: theme.color.primary,
    color: theme.shape.textColor,
    padding: 20,
    margin: 20,
    borderRadius: 4,
  },
});

export default styles;
